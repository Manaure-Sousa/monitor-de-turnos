{
  "name": "monitor-de-turnos",
  "version": "1.0.0",
  "description": "Monitora disponibilidade de turnos e envia alertas por e-mail e Telegram.",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=20.6.0"
  },
  "scripts": {
    "start": "node --env-file=.env monitor.js",
    "check": "node --check monitor.js",
    "check:once": "node --env-file=.env monitor.js --check-once",
    "self-test": "node --env-file=.env monitor.js --self-test"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "nodemailer": "^6.9.16"
  }
}
